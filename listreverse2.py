
# Method 2: Using the reversed() function
my_list = [1, 2, 3] # Assign the list to a variable
reversed_list = list(reversed(my_list)) # Create a reversed iterator and convert it to a list
print(reversed_list) # Print the reversed list